@model IEnumerable<PruebaFinal.Models.Pais>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre_Pais)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre_Pais)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Pais">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Pais">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Pais">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
